[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
markers =
    offline: tests that run without API calls
    integration: tests that require claude CLI
addopts = -v --tb=short -m "not integration"
